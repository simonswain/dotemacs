```bash
git clone git@github.com:simonswain/dotemacs.git .emacs.d
sudo apt-get install python-software-properties
yes|sudo apt-add-repository ppa:cassou/emacs
sudo apt-get update
sudo apt-get install emacs24
emacs
```